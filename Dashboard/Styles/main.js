// The entire JavaScript file for this app. Alpine is the runtime; every piece of
// behaviour lives in the .razor file it belongs to — the auth store and the $api
// magic in App.razor, one Alpine.data component per page.
import './app.css';
import Alpine from 'alpinejs';

window.Alpine = Alpine;
Alpine.start();
