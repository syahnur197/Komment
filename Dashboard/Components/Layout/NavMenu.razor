<div class="flex h-14 items-center border-b border-slate-200 px-4">
    <a class="font-semibold text-slate-900 no-underline" href="">Komment</a>
</div>

@* Base colour is inherited from the nav, not set on the link: a class on the
   link would beat ActiveClass or not depending on Tailwind's output order. *@
<nav class="flex flex-col gap-1 p-3 text-slate-600">
    <NavLink class="flex items-center gap-3 rounded px-3 py-2 text-sm no-underline hover:bg-slate-100 hover:text-slate-900"
             ActiveClass="bg-sky-50 font-medium text-sky-800" href="" Match="NavLinkMatch.All">
        <svg class="size-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3 10.5 12 3l9 7.5M5 9.5V20h14V9.5" />
        </svg>
        Home
    </NavLink>

    @* Revealed by Styles/js/layout.js when a token is present. *@
    <div data-admin-nav hidden>
        <NavLink class="flex items-center gap-3 rounded px-3 py-2 text-sm no-underline hover:bg-slate-100 hover:text-slate-900"
                 ActiveClass="bg-sky-50 font-medium text-sky-800" href="sites">
            <svg class="size-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18Zm0 0c2.5-2.4 3.75-5.4 3.75-9S14.5 5.4 12 3m0 18c-2.5-2.4-3.75-5.4-3.75-9S9.5 5.4 12 3M3.6 9h16.8M3.6 15h16.8" />
            </svg>
            Sites
        </NavLink>
    </div>
</nav>
